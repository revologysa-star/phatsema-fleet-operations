name: Hard fix fleet dashboard (disabled)
on:
  workflow_dispatch:
    inputs:
      reason:
        description: Manual emergency run only
        required: false
        default: disabled
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy dashboard patch workflow disabled; dashboard is maintained in the application source."